name: genshinfan
description: "GenshinFan power by KanThin"
publish_to: 'none'
version: 2.1.0

environment:
  sdk: '>=3.4.0-99.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get:
  get_storage:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
